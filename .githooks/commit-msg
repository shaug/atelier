#!/usr/bin/env bash
set -euo pipefail

message_file=$1
subject=$(head -n 1 "$message_file")

if [[ ! $subject =~ ^(feat|fix|docs|refactor|test|chore|build|ci|perf|style|revert)(\([a-z0-9._-]+\))?:\ .+ ]]; then
  echo "commit subject must use Conventional Commits" >&2
  exit 1
fi
